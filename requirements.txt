numpy
soundfile
pytest
faiss-cpu
# openl3

# Milestone 2 additions
torch>=2.0.0
torchaudio>=2.0.0
demucs>=4.0.0
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
python-multipart>=0.0.6  # Required for FastAPI file uploads
jinja2>=3.1.0  # Required for web UI templates
aiofiles>=23.0.0  # Required for static file serving
scikit-learn>=1.3.0
librosa>=0.10.0
scipy>=1.11.0
pyaudio>=0.2.13
soxr>=0.3.7
# rubberband-python>=0.3.0
soundfile>=0.12.0
pyyaml>=6.0
tqdm>=4.66.0
matplotlib>=3.8.0
seaborn>=0.13.0
# faiss-gpu>=1.7.4  # Optional, use if GPU available

# Music Foundation Models (MERT/MuQ) - Research Paper Requirements
transformers>=4.30.0  # For MERT/MuQ models
accelerate>=0.20.0  # For model acceleration

# Testing dependencies (for local verification)
requests>=2.31.0
requests-toolbelt>=1.0.0  # For upload progress tracking
psutil>=5.9.0

# GUI dependencies (tkinter is built-in with Python, no install needed)